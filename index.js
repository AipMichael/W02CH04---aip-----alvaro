class SkylabArray {
  constructor(height, push, some, find, filter, map) {
    this.length = length;
    this.push = push;
    this.some = some;
    this.find = find;
    this.filter = filter;
    this.map = map;
  }
}
